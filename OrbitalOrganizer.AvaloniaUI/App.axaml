<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="OrbitalOrganizer.App">
    <Application.Styles>
        <SimpleTheme />
        <StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Simple.xaml"/>
        <Style Selector="DataGridRow">
            <Setter Property="MinHeight" Value="30"/>
        </Style>
        <Style Selector="DataGridCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- 12px base font -->
        <Style Selector="Window">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- Groupbox-style HeaderedContentControl -->
        <Style Selector="HeaderedContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border ZIndex="1" Padding="5,0" Margin="5,0,0,0"
                                    Background="{Binding $parent[Window].Background}">
                                <ContentPresenter Content="{TemplateBinding Header}"/>
                            </Border>
                            <Border Grid.RowSpan="2" Grid.ColumnSpan="2" Margin="0,10,0,0"
                                    BorderBrush="Gray" BorderThickness="1" Padding="6">
                                <ContentPresenter Content="{TemplateBinding Content}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Styles>

    <!-- DataGrid colors -->
    <Application.Resources>
        <SolidColorBrush x:Key="DataGridColumnHeaderForegroundBrush" Color="Black"/>
        <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundBrush" Color="#FFEFEFEF"/>
        <SolidColorBrush x:Key="DataGridColumnHeaderHoveredBackgroundBrush" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="DataGridColumnHeaderPressedBackgroundBrush" Color="#FFD0D0D0"/>
        <SolidColorBrush x:Key="DataGridRowSelectedBackgroundBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="DataGridRowSelectedHoveredBackgroundBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="DataGridRowSelectedUnfocusedBackgroundBrush" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="DataGridRowSelectedHoveredUnfocusedBackgroundBrush" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="DataGridRowHoveredBackgroundColor" Color="#FFE8E8E8"/>
    </Application.Resources>
</Application>
