<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Read and parse version from version.txt (e.g., v2.0.2 -> 2.0.2.0). -->
    <VersionFileContent>$([System.IO.File]::ReadAllText('$(MSBuildProjectDirectory)\..\version.txt').Trim())</VersionFileContent>
    <NumericVersion>$([System.Text.RegularExpressions.Regex]::Match('$(VersionFileContent)', '\d+\.\d+(\.\d+)?').Value)</NumericVersion>
    <HasPatch>$([System.Text.RegularExpressions.Regex]::IsMatch('$(NumericVersion)', '\d+\.\d+\.\d+'))</HasPatch>
    <Version Condition="'$(HasPatch)' == 'True'">$(NumericVersion).0</Version>
    <Version Condition="'$(HasPatch)' != 'True'">$(NumericVersion).0.0</Version>
    <AssemblyVersion>$(Version)</AssemblyVersion>
    <FileVersion>$(Version)</FileVersion>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DiscUtils.Iso9660" Version="0.16.13" />
    <PackageReference Include="ByteSize" Version="2.1.2" />
    <PackageReference Include="SharpCompress" Version="0.33.0" />
  </ItemGroup>

  <!-- Native libchdr for dev builds (F5 / dotnet run without a RID).
       Release publish uses libchdr.targets imported by the app projects. -->
  <ItemGroup Condition="'$(RuntimeIdentifier)' == ''">
    <Content Include="runtimes\win-x64\native\libchdr.dll"
             Condition="$([MSBuild]::IsOSPlatform('Windows')) AND Exists('runtimes\win-x64\native\libchdr.dll')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>Never</CopyToPublishDirectory>
      <Link>libchdr.dll</Link>
    </Content>
    <Content Include="runtimes\linux-x64\native\libchdr.so"
             Condition="$([MSBuild]::IsOSPlatform('Linux')) AND Exists('runtimes\linux-x64\native\libchdr.so')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>Never</CopyToPublishDirectory>
      <Link>libchdr.so</Link>
    </Content>
    <Content Include="runtimes\osx-$([System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture.ToString().ToLower())\native\libchdr.dylib"
             Condition="$([MSBuild]::IsOSPlatform('OSX')) AND Exists('runtimes\osx-$([System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture.ToString().ToLower())\native\libchdr.dylib')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>Never</CopyToPublishDirectory>
      <Link>libchdr.dylib</Link>
    </Content>
  </ItemGroup>

  <!-- Regenerate Constants.cs from version.txt before build. Windows only, since
       on Linux/macOS Constants.cs is reused as-is from the last Windows build. -->
  <Target Name="UpdateVersionFromFile" BeforeTargets="BeforeBuild;BeforeRebuild"
          Condition="'$(OS)' == 'Windows_NT'">
    <Exec Command="powershell -ExecutionPolicy Bypass -File &quot;$(MSBuildProjectDirectory)\UpdateVersion.ps1&quot;" />
  </Target>

</Project>
